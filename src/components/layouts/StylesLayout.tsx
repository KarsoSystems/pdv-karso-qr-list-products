import { Box } from "@mui/material";
import styled from "@emotion/styled";

const CenterContainer = styled(Box)(() => ({
  padding: "0px 0px",
  display: "flex",
  justifyContent: "center",
}));

export const StLayout = {
  CenterContainer,
};
